import axios from '@/utils/request'

const api = {
  login: '/user/login',
  register: '/user/register',
  update: '/user/update',
  select: '/user/select',
  selectByCond: '/user/selectByCond',
  upload: '/user/upload'
}

export default api
